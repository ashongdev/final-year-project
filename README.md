# genC

Certificate-generator SaaS app.

- [`client/`](client/) — React + TypeScript + Vite frontend
- [`server/`](server/) — Django REST Framework backend

This repo merges what were previously two separate repositories
(`client` and `server`), preserving the full commit history of each
under their respective directories.
